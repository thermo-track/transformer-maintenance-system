// Fallback options used by the form. If your backend exposes /api/transformers/meta,
// the UI will prefer that response and override these lists.
export const REGION_OPTIONS = ['NORTH','SOUTH','EAST','WEST','CENTRAL'];
export const TYPE_OPTIONS = ['DISTRIBUTION','STEP_UP','STEP_DOWN','OTHER'];